apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ashiorid

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/part-of: ashiorid-ai-manager
  app.kubernetes.io/component: web-frontend
