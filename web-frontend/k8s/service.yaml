apiVersion: v1
kind: Service
metadata:
  name: web-frontend
  namespace: ashiorid
  labels:
    app: web-frontend
    component: frontend
spec:
  type: NodePort
  selector:
    app: web-frontend
  ports:
  - name: http
    port: 8501
    targetPort: 8501
    nodePort: 30501
    protocol: TCP
